<view class="login-prompt card">
  <view class="title">{{title}}</view>
  <view class="desc">{{desc}}</view>
  <button class="primary-btn login-btn" bindtap="handleLogin">{{buttonText}}</button>
</view>
